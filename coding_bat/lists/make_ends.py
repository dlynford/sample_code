# 2/14/2022
# Problem from Codingbat.com
# URL:https://codingbat.com/prob/p124806


def make_ends(nums):
    """Enter an array of integers. The first and last numbers will be printed out."""
    # first = nums[0]
    # last = nums[-1]
    # for i in range(nums):
    #     print(first, last)

make_ends([1,3,5,7])

numbers = [1,2,3,4]

for number in numbers:
    first = numbers[0]
    last = numbers[-1]
print(f"[{first}, {last}]")


# !!This doesnt work
# first = numbers[0]
# last = numbers[-1]
# for number in numbers:
# print(f"[{first}, {last}]")
